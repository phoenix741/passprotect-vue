#import "./line.fragment.graphql"

mutation updateLine($input: WalletLineUpdateInput!) {
  updateLine(input: $input) {
    __typename
    ... on WalletLine {
      ...LineFragment
    }
    ... on Errors {
      errors {
        fieldName
        message
      }
    }
  }
}
