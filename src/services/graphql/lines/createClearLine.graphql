#import "./line.fragment.graphql"

mutation createClearLine($input: ClearLineCreateInput!) {
  createClearLine(input: $input) @client {
    __typename
    ... on WalletLine {
      ...LineFragment
    }
    ... on Errors {
      errors {
        fieldName
        message
      }
    }
  }
}
